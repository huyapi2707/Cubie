// ─── Noise‑gate dB range ────────────────────────────────────────────────────
// Used by the sensitivity slider (device-tab) and the raw level meter.

export const NOISE_GATE_MIN_DB = -100;
export const NOISE_GATE_MAX_DB = 0;

// ─── Boost‑up rate range ────────────────────────────────────────────────────
// Used by the boost-up slider (device-tab).

export const BOOST_UP_MIN = 1;
export const BOOST_UP_MAX = 100;
export const BOOST_UP_STEP = 0.1;
